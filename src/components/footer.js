import React from 'react';
import sandbox from '../images/sandbox.svg'

export const Footer = () => {
  return (
    <footer>
      <img src={ sandbox }/>
      <a href="#">GitHub</a>
    </footer>
  )
}
